Point-of-Sale (POS) SystemProject OverviewThis is a robust, full-stack Point-of-Sale (POS) application built using C# ASP.NET Core 8 MVC, Entity Framework Core (EF Core), and ASP.NET Identity. It is designed to manage product inventory, track sales transactions, and monitor employee performance via role-based access control.The frontend utilizes a mix of Bootstrap and Tailwind CSS utilities for responsiveness and modern aesthetics.Core Features|| Feature Area | Description | Required Role || Authentication | Secure employee login/logout using ASP.NET Identity. | All Users || Role Management | Assigning Manager or Cashier roles to employees. | Manager || Inventory CRUD | Creating, reading, updating, and deleting (CRUD) products. | Manager || POS Terminal | Main sales interface, cart management, cash/card payment processing, and inventory deduction. | Manager, Cashier || Sales Reporting | Aggregated reports showing revenue, transactions, and employee performance. | Manager |🛠️ Technology StackBackend: C# / ASP.NET Core 8 (MVC)Database: SQL Server (via Entity Framework Core)Authentication: ASP.NET Identity (using custom ApplicationUser)Frontend: Razor Views, JavaScript, Bootstrap, Tailwind CSS🚀 Getting StartedFollow these steps to set up and run the project locally.Prerequisites.NET 8 SDK or later.Visual Studio 2022 (recommended) or VS Code.Local SQL Server instance (SQL LocalDB is sufficient for development).1. Database ConfigurationYou must define the database connection string in your appsettings.json file.{
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=POSDb;Trusted_Connection=True;MultipleActiveResultSets=true"
  },
  // ... other settings
}

2. Apply MigrationsThis project uses Entity Framework Core Migrations. The database schema must be created before running the application.Open the Package Manager Console in Visual Studio and run the following commands in sequence:Add-Migration InitialIdentityPosSetup
Update-Database

3. Database SeedingThe application is configured to automatically run the DbInitializer class upon startup (Program.cs). This initializer ensures the following are created:Roles: Manager, CashierDefault Manager Account:Email: admin@pos.comPassword: Pa$$w0rd1 (Change immediately after first login!)Core Endpoints| Feature | URL Path | Required Role || Home/Login | /Identity/Account/Login | Public || POS Terminal | /Sales | Manager, Cashier || Inventory | /Product | Manager || User Management | /UserRole | Manager || Sales Reports | /Report | Manager |# POSSystem
